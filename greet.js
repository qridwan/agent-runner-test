function greet(name) {
  return "Hello, " + name;
}

/**
 * @param {string} name
 * @returns {string}
 */
function farewell(name) {
  return "Goodbye, " + name + "!";
}

module.exports = { greet, farewell };
